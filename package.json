{
  "scripts": {
    "build": "eleventy && yarn build:sass",
    "build:sass": "node-sass sass -o _site",
    "dev:sass": "yarn build:sass --watch",
    "dev": "eleventy --serve & yarn dev:sass"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "@11ty/eleventy-plugin-rss": "^1.0.7",
    "cheerio": "^1.0.0-rc.3",
    "eleventy-plugin-excerpt": "^1.1.2",
    "luxon": "^2.5.2",
    "node-sass": "^4.14.1"
  }
}
